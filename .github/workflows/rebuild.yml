name: Rebuild

on:
  schedule:
  - cron: "0 * * * *"

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - name: Call a webhook
      run: |
        curl -X POST -d {} https://api.netlify.com/build_hooks/5df93745b0342d6b18947ea0
